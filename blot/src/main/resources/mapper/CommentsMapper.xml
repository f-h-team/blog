<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chao.mapper.CommentsMapper">
    <resultMap id="tt" type="comments">
        <id property="id" column="id"/>
        <result column="content" property="content"/>
        <collection property="child" ofType="comments" column="id" select="selectChild"  />
    </resultMap>
    <select id="getRoot" resultMap="tt">
        select id, user_id, content, create_time
        from vueblog.m_comments
        where blog_id = #{blog_id}
          and parent_id IS NULL and status=1
    </select>
    <select id="selectChild" resultType="comments">
        select id, user_id, content, create_time
        from vueblog.m_comments
        where parent_id is not null and parent_id = #{id} and status = 1
    </select>
</mapper>